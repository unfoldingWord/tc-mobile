node_modules
dist
dist-e2e
dev-dist
package-lock.json
*.min.js
*.min.css
.wrangler
.idea
.vscode
.env*
!.env.example

# Capacitor native projects (#262). Generated and managed by the `cap` CLI —
# Java/Kotlin/Swift/Gradle plus generated JSON and the copied web bundle. Not
# this repo's JS to format; `cap sync` regenerates the copied assets.
android
ios

# Fastlane config is Ruby (#262); prettier has no parser for it, and its
# generated report/README should not be formatted either.
fastlane
Gemfile
Gemfile.lock

# Vendored third-party source (#36), kept byte-for-byte as fetched — see
# third_party/lamejs-1.2.7/PROVENANCE.md. Formatting it would modify it.
third_party
